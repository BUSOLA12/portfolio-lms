Mark step $ARGUMENTS complete in docs/build-plan.md.

Append " — **DONE**" to its heading, replace the estimated file list with the
files that actually exist, and add the verification result. Record any decision
made during the step under a "Decided during the step" bullet.

Change nothing else in the file. Do not mark any other step done.
